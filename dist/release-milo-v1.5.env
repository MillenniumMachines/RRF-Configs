#!/usr/bin/env bash
TG_RELEASE="3.5.0-rc.3%2B101"
DUET_RELEASE="3.5.0-rc.3"

# This is annoying but GitHub creates the release tag from the version
# tag, so the release cannot be found using the version directly.
MOS_RELEASE_TAG="release-62a501a0dbc4f77a1a020a5123a5c99dc2be855d"
MOS_RELEASE="v0.2.1-rc3"

TG_RELEASE_URL="${TEAMGLOOMY_RELEASES}/v${TG_RELEASE}"
DUET_RELEASE_URL="${DUET_RELEASES}/${DUET_RELEASE}"
MOS_RELEASE_URL="${MILLENNIUMOS_RELEASES}/${MOS_RELEASE_TAG}"

MOS_SRC_NAME="mos-release-${MOS_RELEASE}.zip"
DWC_SRC_NAME="DuetWebControl-SD.zip"

DWC_DST_NAME="dwc-${DUET_RELEASE}.zip"
MOS_DST_NAME="mos-${MOS_RELEASE}.zip"

RRF_FIRMWARE_DST_1_NAME="firmware.bin"

WIFI_FIRMWARE_SRC_NAME="DuetWiFiServer_32-2.1beta6-01.bin"
WIFI_FIRMWARE_DST_NAME=DuetWiFiServer.bin
WIFI_FIRMWARE_URL="${TG_RELEASE_URL}/${WIFI_FIRMWARE_SRC_NAME}"

MOS_URL="${MOS_RELEASE_URL}/${MOS_SRC_NAME}"
DWC_URL="${DUET_RELEASE_URL}/${DWC_SRC_NAME}"
