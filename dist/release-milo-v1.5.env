#!/usr/bin/env bash
TG_RELEASE="3.6.1"
DUET_RELEASE="3.6.1"
MOS_RELEASE="0.5.0"

TG_RELEASE_URL="${TEAMGLOOMY_RELEASES}/v${TG_RELEASE}"
DUET_RELEASE_URL="${DUET_RELEASES}/${DUET_RELEASE}"
MOS_RELEASE_URL="${MILLENNIUMOS_RELEASES}/v${MOS_RELEASE}"

MOS_SRC_NAME="MillenniumOS-v${MOS_RELEASE}.zip"
DWC_SRC_NAME="DuetWebControl-SD.zip"

DWC_DST_NAME="dwc-${DUET_RELEASE}.zip"
MOS_DST_NAME="mos-v${MOS_RELEASE}.zip"

RRF_FIRMWARE_ZIP_NAME="STM32RepRapFirmwareWiFi.zip"

RRF_FIRMWARE_URL="${TG_RELEASE_URL}/${RRF_FIRMWARE_ZIP_NAME}"

RRF_FIRMWARE_SRC_NAME="firmware_${RRF_BOARD_TYPE}.bin"
RRF_FIRMWARE_DST_NAME="firmware.bin"

WIFI_FIRMWARE_DST_NAME="WiFiModule_${WIFI_BOARD_TYPE}.bin"
WIFI_FIRMWARE_SRC_NAME="WiFiModule_${WIFI_BOARD_TYPE}.bin"

MOS_URL="${MOS_RELEASE_URL}/${MOS_SRC_NAME}"
DWC_URL="${DUET_RELEASE_URL}/${DWC_SRC_NAME}"
